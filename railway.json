{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd frontend && npm install && npm run build && cp -r dist ../backend/dist && cd ../backend && npm install"
  },
  "deploy": {
    "startCommand": "cd backend && node server.js"
  }
}
